# Production database URL. DATABASE_URL is preferred.
# POSTGRES_URL also works when a provider such as Vercel Marketplace injects it.
# Example providers: Neon, Supabase, Vercel Marketplace Postgres, Railway Postgres.
DATABASE_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
